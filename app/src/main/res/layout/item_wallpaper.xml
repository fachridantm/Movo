<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="320dp"
    android:layout_height="180dp"
    android:layout_marginStart="24dp"
    app:cardCornerRadius="10dp"
    app:cardElevation="0dp">

    <ImageView
        android:id="@+id/iv_wallpaper"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@string/content_desc_wallpaper_movie"
        android:scaleType="centerCrop"
        android:src="@drawable/img_wallpaper_sample" />

    <TextView
        android:id="@+id/tv_wallpaper_is_empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:gravity="center"
        android:fontFamily="@font/poppins_bold"
        android:background="@color/gray_genre_text"
        android:text="@string/wallpapers_is_empty"
        android:textColor="@color/app_backround"
        android:textSize="16sp"
        android:visibility="gone"
        tools:visibility="gone" />

</com.google.android.material.card.MaterialCardView>