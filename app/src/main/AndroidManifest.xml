<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission-sdk-23 android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name="id.outivox.movo.MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/logo_home_movo"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:roundIcon="@drawable/logo_home_movo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Movo"
        tools:targetApi="m">
        <activity
            android:name="id.outivox.movo.presentation.detail.DetailActivity"
            android:exported="false"
            android:theme="@style/transparentStatusBar" />
        <activity
            android:name="id.outivox.movo.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>